# Constraint Lab

Constraint Lab is an interactive accessibility proofing simulator for checkout experiences.

It lets you test a synthetic checkout under keyboard-only navigation, low visual access, and a slow or unreliable connection.

## Run locally

```bash
npm install
npm run dev
```
